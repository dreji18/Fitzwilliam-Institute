# lab_06_exercises.py
# Python 101 — Week 06: Lists & Tuples
# Fitzwilliam Institute

# ── Exercise 1 ────────────────────────────────────────────────
# Given the list below:
#   a) Print the first and last item
#   b) Print the last 3 items using slicing
#   c) Print every second item
scores = [88, 72, 95, 61, 84, 77, 90, 55, 68, 91]


# ── Exercise 2 ────────────────────────────────────────────────
# Start with an empty list called `shopping`.
#   a) Add: "milk", "eggs", "bread", "butter"
#   b) Insert "coffee" at position 1
#   c) Remove "bread"
#   d) Sort the list alphabetically
#   e) Print the final list


# ── Exercise 3 ────────────────────────────────────────────────
# Use a list comprehension to create a new list from scores (above) that:
#   - Contains only scores above 75
# Print the filtered list.
scores = [88, 72, 95, 61, 84, 77, 90, 55, 68, 91]


# ── Exercise 4 ────────────────────────────────────────────────
# Use a list comprehension to create a list of the squares of 1 through 10.
# Print the result.


# ── Exercise 5 ────────────────────────────────────────────────
# A tuple holds a student record: (name, age, course)
# Unpack it into three separate variables and print a sentence about the student.
student = ("Emma", 21, "Python 101")


# ── Exercise 6 ────────────────────────────────────────────────
# Flatten this list of lists into a single flat list using a list comprehension.
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]


# ── Exercise 7 (Stretch) ──────────────────────────────────────
# Remove all duplicates from the list below WITHOUT using set().
# Use a loop and an empty list to build the result.
# Print the deduplicated list preserving original order.
dupes = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5]
