# lab_05_exercises.py
# Python 101 — Week 05: Functions
# Fitzwilliam Institute

# ── Exercise 1 ────────────────────────────────────────────────
# Write a function called greet(name) that prints "Hello, [name]!"
# Call it with your own name.


# ── Exercise 2 ────────────────────────────────────────────────
# Write a function called celsius_to_fahrenheit(c) that:
#   - Takes a temperature in Celsius
#   - Returns the Fahrenheit equivalent: F = (C * 9/5) + 32
# Print the result for 0°C, 100°C, and 37°C (body temperature).


# ── Exercise 3 ────────────────────────────────────────────────
# Write a function called calculate_area(shape, a, b=None) that:
#   - Returns the area of a "rectangle" (a * b) or "triangle" (0.5 * a * b)
#   - Returns None for unknown shapes
# Test it with both shapes.


# ── Exercise 4 ────────────────────────────────────────────────
# Write a function called is_prime(n) that returns True if n is prime.
# A number is prime if it's only divisible by 1 and itself.
# Hint: loop from 2 to n-1 and check for divisors.
# Test it on: 2, 7, 9, 13, 25


# ── Exercise 5 ────────────────────────────────────────────────
# Write a function called summarise(numbers) that takes a list of numbers and
# returns a dictionary with keys: "count", "sum", "min", "max", "average"
# Test it on [4, 8, 15, 16, 23, 42]


# ── Exercise 6 (Stretch) ──────────────────────────────────────
# Write a function called fizzbuzz(n) that returns a list of FizzBuzz strings
# for numbers 1 through n (using the classic rules).
# Print the result for n=20.
